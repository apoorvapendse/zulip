* [`POST /register`](/api/register-queue): `realm_giphy_rating`
  is renamed to `realm_gif_rating` as it is the common rating
  configured for both Tenor and GIPHY integrations.
* [`GET /events`](/api/get-events), `PATCH /realm`: `giphy_rating`
  is now renamed to `gif_rating` as it is the common rating configured
  for both Tenor and GIPHY integrations.
